# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Define aliases.
{% if zshrc_aliases %}
{% for name in zshrc_aliases %}
alias {{ name }}={{ zshrc_aliases[name] }}
{% endfor %}
{% endif %}
